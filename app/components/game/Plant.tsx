

export default function Plant({className}:{className?:string}) {
  return (
    <section className={`bg-[radial-gradient(ellipse_at_top_right,_var(--tw-gradient-stops))]
from-[#00a388]
via-[#79bd8f]
to-[#beeb9f] h-full ${className} p-4`}></section>
  )
}
